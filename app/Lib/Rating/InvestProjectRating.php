<?php
App::uses('AbstractRating', 'Lib/Rating');

class InvestProjectRating extends AbstractRating {
	/*
	//Currently Invest projects not related to group
	protected $_config = array(
		'add' => array(
			'label' => 'Invest project creation',
			'createdOnly' => true,
			'target' => array(
				'Group' => 'group_id'
			),
			'value' => 5
		),
	);
	*/
}
